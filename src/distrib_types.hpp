#pragma once

namespace distrib {
    struct exponential_t {
        double value;
    };

    struct gamma_t {
        double value;
    };

    struct constant_t {
        const double value;
    };
};  // namespace distrib